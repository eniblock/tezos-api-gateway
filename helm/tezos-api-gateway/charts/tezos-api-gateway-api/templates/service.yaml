apiVersion: v1
kind: Service
metadata:
  name: {{ include "tezos-api-gateway-api.fullname" . }}
  labels:
    {{- include "tezos-api-gateway-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3333
      protocol: TCP
      name: http
  selector:
    {{- include "tezos-api-gateway-api.selectorLabels" . | nindent 4 }}
