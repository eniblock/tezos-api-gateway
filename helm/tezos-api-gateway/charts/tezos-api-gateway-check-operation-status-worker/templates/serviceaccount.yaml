{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tezos-api-gateway-check-operation-status-worker.serviceAccountName" . }}
  labels:
    {{- include "tezos-api-gateway-check-operation-status-worker.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
