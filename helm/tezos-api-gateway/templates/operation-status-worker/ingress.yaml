{{- if .Values.operationStatusWorker.ingress.enabled -}}
{{- $fullName := include "tezos-api-gateway.fullname" . -}}
{{- $svcPort := .Values.operationStatusWorker.service.port -}}
{{- if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-operation-status-worker
  labels:
    {{- include "tezos-api-gateway.labels" . | nindent 4 }}
  {{- with .Values.operationStatusWorker.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.operationStatusWorker.ingress.tls }}
  tls:
    {{- range .Values.operationStatusWorker.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.operationStatusWorker.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            backend:
              serviceName: {{ $fullName }}-operation-status-worker
              servicePort: {{ $svcPort }}
          {{- end }}
    {{- end }}
  {{- end }}
