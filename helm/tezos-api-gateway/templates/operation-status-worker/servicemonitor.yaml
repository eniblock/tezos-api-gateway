{{- if and .Values.operationStatusWorker.enabled .Values.operationStatusWorker.metrics.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "tezos-api-gateway.fullname" . }}-operation-status-worker
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tezos-api-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: operation-status-worker
spec:
  selector:
    matchLabels:
      {{- include "tezos-api-gateway.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: operation-status-worker
  endpoints:
    - port: prometheus
      {{- if .Values.operationStatusWorker.metrics.interval }}
      interval: {{ .Values.operationStatusWorker.metrics.interval }}
      {{- end }}
      {{- if .Values.operationStatusWorker.metrics.scrapeTimeout }}
      scrapeTimeout: {{ .Values.operationStatusWorker.metrics.scrapeTimeout }}
      {{- end }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
{{- end }}
