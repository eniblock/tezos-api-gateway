export enum TezosDataType {
  ADDRESS = 'address',
  BOOLEAN = 'bool',
  BYTES = 'bytes',
  CONTRACT = 'contract',
  INTEGER = 'int',
  LIST = 'list',
  MAP = 'map',
  NATURAL = 'nat',
  STRING = 'string',
  UNIT = 'unit',
}
